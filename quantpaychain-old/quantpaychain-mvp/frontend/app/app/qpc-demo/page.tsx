/**
 * Página de demostración del núcleo QPC v2
 * Ruta: /qpc-demo
 */

import { QPCDashboard } from '@/components/qpc/QPCDashboard';

export default function QPCDemoPage() {
  return <QPCDashboard />;
}
